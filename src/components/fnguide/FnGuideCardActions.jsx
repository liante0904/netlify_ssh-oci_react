export default function FnGuideCardActions({ item }) {
  return <div className="card-actions">{item.pdf_url && <a href={item.pdf_url} target="_blank" rel="noopener noreferrer" className="pdf-action-btn pdf-action-primary" title="PDF 원문 보기"><span aria-hidden="true">📄</span> PDF 보기</a>}{(item.pdf_url || item.article_url) && <a href={item.pdf_url || item.article_url} target="_blank" rel="noopener noreferrer" className="pdf-action-btn pdf-action-fnguide">↗ FnGuide 원문 보기</a>}{item.sec_reports?.map((secReport) => { const reportUrl = secReport.pdf_file_url || secReport.telegram_url; if (!reportUrl) return null; return <a key={secReport.report_id} href={reportUrl} target="_blank" rel="noopener noreferrer" className="pdf-action-btn sec-pdf-btn" title={`${secReport.firm_nm}: ${secReport.article_title}`}>📄 {secReport.firm_nm} 원본 PDF 보기</a>; })}</div>;
}
