import React from 'react';

export default function TelegramUserPanel({ user, botName, onLogout, onOpenSettings, theme, themePreference, onToggleTheme }) {
  const modeLabel = themePreference === 'system' ? `시스템 설정 (${theme === 'dark' ? '다크' : '라이트'})` : `${theme === 'light' ? '라이트' : '다크'} 모드 · 다음: ${theme === 'light' ? '다크' : '시스템'}`;
  return <div className="telegram-user-card"><div className="user-info-header"><span className="user-name">👤 {user.first_name}님 <small className="telegram-user-id">(ID:{user.id})</small></span><button className="logout-small-btn" onClick={onLogout}>로그아웃</button></div><div className="bot-connect-banner"><a href={`https://t.me/${botName}?start=${user.id}`} target="_blank" rel="noopener noreferrer" className="bot-connect-btn"><span className="icon">🚀</span> 텔레그램 봇 연결하기 (최초 1회 필수)</a></div><button type="button" className="account-settings-btn" onClick={onOpenSettings}><span className="menu-setting-icon">🔔</span><span><strong>내 설정</strong><small>키워드와 텔레그램 알림 관리</small></span></button><button type="button" className="account-settings-btn" onClick={onToggleTheme}><span className="menu-setting-icon">{themePreference === 'system' ? '🖥️' : theme === 'light' ? '🌙' : '☀️'}</span><span><strong>화면 모드</strong><small>{modeLabel}</small></span></button></div>;
}
