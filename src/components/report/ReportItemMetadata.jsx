export default function ReportItemMetadata({ firm, title, finalLink, showFirmTag, hasSummary, hasFnguideSummary, onToggleSummary, id, hasDirectSignal, hasUnverifiedValuation, rating, formattedTargetPrice, revisionType, reportType, stockTickers, visibleTags, onTagClick }) {
  return <>
    <div className="report-header">{showFirmTag && <span className="firm-tag">{firm}</span>}<div className="report-title-container"><a href={finalLink} target="_blank" rel="noopener noreferrer" className="report-title">{title}</a>{hasSummary && <span className="ai-badge" onClick={() => onToggleSummary(id)}>AI 요약</span>}{hasFnguideSummary && <span className="ai-badge fnguide-badge-title" onClick={() => onToggleSummary(id)}>FnGuide 요약</span>}</div></div>
    {hasDirectSignal && <div className="report-signals" aria-label="리포트 투자 신호">{hasUnverifiedValuation && rating && <span className="signal signal-rating">출처 확인 필요 · 의견 {rating}</span>}{hasUnverifiedValuation && formattedTargetPrice && <span className="signal signal-target">출처 확인 필요 · 목표가 {formattedTargetPrice}</span>}{hasUnverifiedValuation && revisionType && <span className="signal signal-revision">출처 확인 필요 · {revisionType}</span>}{reportType && <span className="signal signal-type">{reportType}</span>}{stockTickers.map((ticker) => <span key={`ticker-${ticker}`} className="signal signal-ticker">{ticker}</span>)}</div>}
    {visibleTags.length > 0 && <div className="report-tags">{visibleTags.map(({ value, type }, index) => <button type="button" key={`${type}-${index}`} className={`tag tag-${type}`} onClick={() => onTagClick?.(value, type)}>{value}</button>)}</div>}
  </>;
}
