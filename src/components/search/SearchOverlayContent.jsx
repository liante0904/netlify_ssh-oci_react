import CompanySelect from '../CompanySelect';
import BoardSelect from '../BoardSelect';

export default function SearchOverlayContent({ overlayRef, inputRef, category, query, boards, selectedCompanyOrder, selectedBoard, toast, onClose, onCategory, onQuery, onKeyDown, onSearch, onCompany, onBoard }) {
  return <><div className="search-overlay visible" id="searchOverlay" onClick={onClose}><div ref={overlayRef} className="search-container" role="dialog" aria-modal="true" aria-labelledby="search-overlay-title" onClick={(event) => event.stopPropagation()}><h2 id="search-overlay-title" className="sr-only">리포트 검색</h2><select id="searchCategory" className="search-category" value={category} onChange={onCategory}><option value="title">제목</option><option value="writer">작성자</option><option value="tags">태그</option><option value="sector">산업</option><option value="stock">종목명</option><option value="company">증권사</option></select>{category === 'company' ? <div className="search-company-filter"><CompanySelect value={selectedCompanyOrder} onChange={onCompany} className="company-select" />{selectedCompanyOrder && <BoardSelect value={selectedBoard} boards={boards} onChange={onBoard} className="board-select-overlay" />}</div> : <input ref={inputRef} type="text" id="searchInput" placeholder="검색어 입력..." className="search-input" value={query} onChange={onQuery} onKeyDown={onKeyDown} />}{category !== 'company' && <button className="search-submit" onClick={onSearch}>검색</button>}</div></div><div className={`toast-container ${toast.visible ? 'visible' : ''}`}>{toast.message}</div></>;
}
